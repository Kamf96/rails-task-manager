<h1>Tares</h1>
<ul>
<% @tasks.each do |task| %>
  <li><%= link_to "#{task.title}", task_path(task["id"]) %>
  <%= link_to "Destroy #{task.title}", task_path(task), method: :delete, data: { confirm: "Are you sure?" } %></li>
  <% end %>
</ul>

<%= link_to "Add new task", new_task_path %>
